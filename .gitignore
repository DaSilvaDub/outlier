.pytest_cache/
__pycache__/
*.py[cod]
.venv/
venv/
*.log
*.log.err

# Stray debug/scratch/report artifacts previously committed by accident at repo
# root. Removed from tracking; ignored here so they don't get re-added.
.tmp_pytest_*.txt
scratch_*.py
login_page.html
totals_output.txt
betting_report_*.md
terminals/
alt.csv
parlays.csv
dashboard.html
do_refactor.py
generate_refactor.py
new_write_pack.py
original_write_pack.py

# Standalone Outlier auth/session material. Do not commit secrets.
config/*
!config/.gitkeep
!config/blend_promotion.json
!config/ncaa_pipeline.json
config/outlier_session.json
config/.outlier_session/
config/api_bearer_token.txt
config/api_request_headers.json

# Live scraper outputs.
data/*
!data/.gitkeep
packs/ 

# Permanent local feedback data and generated calibration reports.
calibration/*.sqlite3
calibration/*.sqlite3-shm
calibration/*.sqlite3-wal
calibration/reports/
calibration/exports/
calibration/settlements/generated/
# Backfilled projection training samples: large and fully regenerable from
# `python -m outlier_scrapers.projections backfill`. Fitted model/validation
# JSON under calibration/projections/ stays committable (small + audited).
calibration/projections/*.jsonl

# Local credentials for headless auth - never commit.
.env
*.env
*.egg-info/

copilot-worktrees/

# Local tooling and throwaway debug/scratch work (screenshots, debug scripts).
.codex/
scratch/

# AI agent session logs / introspection (noise across ents, not part of source)
.agent-log/

# Local hookify rules (personal/local dev-time guardrails, not shared source)
.claude/*.local.md

.tmp.driveupload/

# auto-added 2026-08-23 : local calibration DB artifacts (exceed GitHub 100MB limit)
calibration/*.db
calibration/*.sqlite3*
calibration/.fuse_hidden*
today_settlements.csv/
