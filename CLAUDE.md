# Claude — start here

You are agent **`claude`** in this repo. Multiple AI agents share this codebase.

1. **Read `AGENTS.md` first** — especially the "Multi-Agent Sync Protocol"
   section. It is mandatory.
2. On session start: read `.agent-log/SUMMARY.md`, the latest `.agent-log/`
   entries, and `git log --oneline -15`.
3. On session end: commit your work with an `Agent: claude` trailer, write a
   session note in `.agent-log/`, and update `.agent-log/SUMMARY.md`.

All project rules, scope constraints, and technical gotchas live in `AGENTS.md`.
