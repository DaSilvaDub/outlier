1. **Last Commit SHA**: `c3dcff3` (branch `claude/totals-pipeline-audit-uwjp1a`)
2. **Files Touched**: `outlier_scrapers/probability_blend.py`, `outlier_scrapers/feedback.py`, `tests/test_probability_blend.py`, `tests/test_feedback.py`, and this handoff file. PR: `https://github.com/DaSilvaDub/outlier/pull/129`
3. **Next Steps**: Phase 1 of a totals-pipeline audit ("stop the bleeding" on the market/model blend). `fit_weight_artifact` had no chronological holdout — it fit purely in-sample, letting thin segments (e.g. `data_quality_tier=MEDIUM`, n=74) drift to `raw_market_weight=0.0`, fully overriding market consensus on noise. Added a per-segment chronological train/holdout split: a segment only unlocks a weight below the new `DEFAULT_MARKET_WEIGHT_FLOOR` (0.75) once it beats the market-only Brier score OOS; otherwise it's floored and the raw in-sample fit is kept separately as `raw_market_weight` for visibility. Also quarantined the known 2026-07 column-misalignment rows (`market_type`/`time_before_game` landing as stringified percentiles `'50.0'`/`'25.0'`/`'75.0'`) from training, and raised `min_samples` from 30 to 200 to match real segment sizes in the ledger. Note: the blend was already shadow-only (`config/blend_promotion.json`, `min_eligible_samples=1000`) so it wasn't driving live sizing, but the artifact itself needed to be trustworthy before any promotion decision. `calibration/blend_weights.json` is generated and will pick up the new logic on the next scheduled `feedback fit-blend` refit — not hand-edited here. Remaining phases from the audit (OOS scoring harness, `ProjectionDistribution` extended to game totals, fundamentals model) are not started. Verified with `pytest tests/test_probability_blend.py tests/test_feedback.py tests/test_pack.py tests/test_stake_calibration.py tests/test_learned_multipliers.py tests/test_projection_training.py` (247 passed) and `ruff check`.
