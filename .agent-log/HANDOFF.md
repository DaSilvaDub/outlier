- **Last Commit SHA**: bc123b7 (branch `claude/fix-this-3b8abd`, [PR #107](https://github.com/DaSilvaDub/outlier/pull/107)) merged with `origin/master` (which had since picked up PR #108, 266ccc8, merged 2026-08-23)
- **Files Touched (PR #107)**: outlier_scrapers/probable_pitchers.py, outlier_scrapers/refresh.py, outlier_scrapers/projections.py, outlier_scrapers/paths.py, outlier_scrapers/daily_job.py, tests/test_probable_pitchers.py, tests/test_projections.py
- **PR #107 summary**: Fixed both halves of an audit finding that `independent_model_prob` coverage on MLB SO rows was near-zero in production. (1) Root cause: `probable_pitchers.py` had no auto-advance-to-tomorrow logic like `games.py`, so a nightly refresh run late at night kept matching SO markets against yesterday's already-finished slate's starters. Fixed with `_slate_is_complete()` + auto-advance (later hardened per PR review: detailedState/GAMEOVER/COMPLETEDEARLY fallback, empty-slate semantics matching games.py, malformed-payload safety), and `refresh.py` no longer drops an explicit `--date` pin for this producer. Verified live: SF's 2026-08-23 starter resolved correctly and independent_model_prob went 0/6 → 5/6 after a pack rebuild. (2) The file-based projection artifact (`<sport>_projections_latest.json` / `packs/<date>/projections.jsonl`, and the `backfill`/`train`/`validate`/`project` CLI) was genuinely dead — nothing ever invoked it. Wired it up: `export_projections()`/`build_mlb_so_projections()` compute starter-SO projections from the already-refreshed props + probable-pitchers files, reusing the same `mlb_so_projection_record()` pack.py falls back to inline. New `export` CLI subcommand, `--projections` refresh.py flag, daily_job.py pipeline step. Verified against real data: 343 SO rows → 314 gamelog-eligible + 29 audit-only-stub projections, matching confirmed-starter coverage exactly.
- **Concurrent PR #108** (merged into master while #107 was in flight, per the prior handoff entry): fixed the pre-existing `totals_model.py`/`pack.py`/`projections.py` mypy errors and the `test_write_pack_emits_player_and_league_bankroll_csvs` pytest failure that PR #107's CI runs were also seeing (confirmed unrelated to #107 at the time via `git stash`). #107's `projections.py` additions and #108's mypy fix landed on non-overlapping lines and merged cleanly with no conflicts; only this HANDOFF.md file conflicted (both PRs updated it), resolved by combining both entries.
- **Next Steps**: Re-run full test suite + mypy after this merge to confirm the combined state (#107 + #108) is fully green before finalizing PR #107's merge to master, since the earlier "pre-existing failure" analysis for #107 was based on a master snapshot that has since moved.
