Last Commit SHA: f666df252c37a06b12ecff9baacae9c9db3f3f8
Files Touched: outlier_scrapers/pack.py, outlier_scrapers/pack_market.py (new), outlier_scrapers/pack_projections.py (new), outlier_scrapers/pack_sizing.py (new), outlier_scrapers/pack_ranking.py (new), outlier_scrapers/pack_context.py (new), outlier_scrapers/pack_render.py (new), outlier_scrapers/pack_publish.py (new), outlier_scrapers/pack_selection.py (new)
Next Steps: Implemented PR 4 of the decomposition plan (pack.py breakup) on branch claude/pack-py-decomposition-isdk29, PR: https://github.com/DaSilvaDub/outlier/pull/134. Split the 3,305-line pack.py monolith into eight focused modules — pack_market (odds/market resolution + formatting), pack_projections (independent projections + probability blend), pack_sizing (stake sizing, delegates to portfolio.py/sizing.py/learned_multipliers.py), pack_ranking (board ranking/quota fill), pack_context (event/injury/freshness enrichment), pack_render (briefing/dossier markdown), pack_publish (write_pack + portfolio-risk sidecar + atomic staged-pack swap/rollback), and pack_selection (candidate row policy — build_row is now an orchestrator over per-phase helpers: _resolve_candidate_identity, _build_base_row, _apply_price_and_sizing, _apply_projection_fields, _apply_quality_and_signal_flags, _finalize_actionable_row). pack.py is now a compatibility facade re-exporting every name (via explicit __all__, since most are underscore-prefixed) so all existing `from outlier_scrapers.pack import X` / `pack.X` callers and test monkeypatches keep working; it still owns process_stream/select_date/_expected_pack_slate_date/build_pack_with_coverage/build_pack/main. All logic relocated verbatim — no pack/desk behavior changed. Verified: full pytest suite (1290 passed, same 14 pre-existing failures as baseline from missing openai/anthropic/google.genai/dateutil packages in this sandbox, confirmed via git stash against the pre-change tree), tests/test_pack.py 146 passed, ruff clean, mypy same baseline errors only, pyright clean (0 errors, after two narrowing-only local-variable tweaks in pack_selection.py that don't change runtime behavior). PR #134 opened against master and this session is subscribed to its activity. Not started: PR 5 (feedback.py decomposition) — feedback.py is still a single module; that work is explicitly scoped as a separate follow-up PR per the plan and was not touched here.
