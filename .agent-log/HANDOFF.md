Last Commit SHA: 5b696dbb1afc8607689070aec35d5a301f8fafd8
Files Touched: outlier_scrapers/run_desk.py, outlier_scrapers/desk_snapshot.py, tests/test_run_desk.py, tests/test_desk_snapshot.py
Next Steps: Landed PR2 (parallel A/B/C/D desk execution) on top of PR1's publication-validation baseline (c1e4f77), on branch claude/codex-last-season-usage-d1c712, pushed to origin -- NOT yet opened as a PR (gh CLI is not installed and GitHub MCP tooling requires OAuth not available in this environment; a human needs to run `gh pr create` from that branch, or open https://github.com/DaSilvaDub/outlier/pull/new/claude/codex-last-season-usage-d1c712).

What changed:
- run_desk.py: run_phase() now returns a typed StageResult (stage_result.py, already present from PR1) instead of a dict, deciding caching/success from PR1's validate_current_publication()/expected_request_sha256() helpers rather than legacy chatgpt_a.md/gemini_b.md/claude_d.md/claude_e.md existence. Added run_independent_phases() (bounded ThreadPoolExecutor, default max_workers=4, new --max-workers CLI flag, canonical A,B,C,D return order, one failure never blocks the others). orchestrate_desk() decomposed into validate_desk_request/build_initial_status/run_independent_phases/run_synthesis_phase/advance_snapshot/resolve_final_report/compute_overall_state, composed in ~50 lines.
- desk_snapshot.py: added upstream_ready_for_e() (thin adapter over desk_publication_ready()) and validate_snapshot_for_final_report() (snapshot-first final-report authority: validates desk_snapshot.json against the live pack fingerprint and each pinned publication's own manifest/hash integrity, never a pass-level current.json pointer). The in-process RLock serializing concurrent publications (_IN_PROCESS_PUBLICATION_LOCK wrapping fingerprint_locks/writer_lock_only) already existed from PR1/c1e4f77 -- confirmed via a new regression test, no code change needed there.
- Tests: 7 new run_desk regressions (barrier-proven real concurrency, one-failure-doesn't-block-others, uncaught-worker-exception, forced-hash-not-a-cache-hit, stale-publications-cannot-unlock-E, optional-C-never-blocks-E, E-only-after-gate, stale-E-markdown-not-authoritative) and 4 new desk_snapshot regressions (live-hash-mismatch invalidates authority, E pinned to obsolete upstream rejected, forced same-request republish preserves publication identity, parallel publication serialization). Existing high-level orchestration tests rewritten to publish real gate-clean envelopes via runner_common's publish_verdict_pass/publish_finding_pass/publish_reconciliation_pass (matching each phase module's real expected_request_sha256()) instead of only writing legacy Markdown, since success is no longer Markdown-based.

Verification: pytest (uv-installed pytest at /root/.local/bin, PYTHONPATH-joined with the base interpreter's site-packages since project deps and pytest live in different environments here) shows the same 10 pre-existing failures as master (missing openai/anthropic/google-genai SDKs in this sandbox; confirmed identical via `git stash`), 22 more tests passing than baseline, zero regressions. ruff clean. mypy/pyright show the same pre-existing findings as master with zero new issues in changed files. No reasoning-model/provider calls were made (offline throughout, per house rule).

Next agent: open the PR from claude/codex-last-season-usage-d1c712 to master, then verify CI passes with real SDKs installed (this sandbox could not install openai/anthropic/google-genai -- no network egress to PyPI was available here beyond the pre-cached uv tool wheels for pytest/ruff/mypy/pyright).
