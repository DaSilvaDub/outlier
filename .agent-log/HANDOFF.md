- **Last Commit SHA**: fabb42071ea4b7f5f5527e18a2b45cd0d5e5bb90 (branch `claude/fix-this-3b8abd`, [PR #107](https://github.com/DaSilvaDub/outlier/pull/107))
- **Files Touched**: outlier_scrapers/probable_pitchers.py, outlier_scrapers/refresh.py, tests/test_probable_pitchers.py
- **Next Steps**: Fixed the root cause behind an audit finding that `independent_model_prob` coverage on MLB SO rows was near-zero in production. The real bug was NOT that `outlier_scrapers.projections` is never invoked directly (that's true but a separate, already-dead file-based path via `<sport>_projections_latest.json`/`projections.jsonl` — `pack.py` never reads from it in the hot path). The actual live mechanism is `pack.py`'s inline `mlb_so_projection_record()`, which matches SO markets against `probable_pitchers_latest.json`. That file had no auto-advance-to-tomorrow logic like `games.py` does, so a nightly refresh run late at night (after today's slate finished) kept fetching *today's* completed schedule while props/cards/games had already rolled onto tomorrow's board — so starters never matched by name. Fixed by adding the same auto-advance check to `probable_pitchers.py` and by having `refresh.py` stop silently dropping an explicit `--date` pin for this producer. Verified live: SF's 2026-08-23 starter now resolves correctly ("Matt Wilkinson") and independent_model_prob coverage on today's SO board went 0/6 → 5/6 after rebuilding the pack. PR #107 is open against master, not yet merged. The dead file-based `projections.jsonl`/CLI `backfill`/`train`/`validate` path (still stub-only) was intentionally left alone — out of scope for this fix, flagged in the PR description for awareness.
