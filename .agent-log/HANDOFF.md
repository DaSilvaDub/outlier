- **Last Commit SHA**: (pending push) on `claude/goal-resolution-dra9j0`
- **Files Touched**: outlier_scrapers/projection_training.py (new), outlier_scrapers/projections.py, outlier_scrapers/probability_blend.py, outlier_scrapers/pack.py, outlier_scrapers/daily_job.py, config/blend_promotion.json (new), .gitignore, AI-research-desk-runbook.md, docs/plans/2026-08-24-projection-calibration-loop.md (new), tests/test_projection_training.py (new), tests/test_blend_promotion.py (new), tests/test_projections.py, tests/test_pitcher_so_features.py, tests/test_daily_job.py
- **Next Steps**: Tier-3 items 8–11 are implemented (see docs/plans/2026-08-24-projection-calibration-loop.md). Remaining work is data, not code: (1) run `projections backfill --sport MLB --from 2024-03-20 --to <today>` against the live MLB Stats API, then `train`/`validate`, and only `promote` on a passing report; (2) let `daily_job` accumulate settled rows until `blend_weights.json` `eligible_samples` clears `config/blend_promotion.json`'s `min_eligible_samples` (1000) before considering `mode: live`. Neither the backfill nor the blend refit could be executed in the remote session (no MLB Stats API egress, no feedback ledger); everything is covered by offline tests with injected fetchers.
