1. **Last Commit SHA**: `04b5fe2` (squash-merged to `master` from branch `claude/totals-pipeline-audit-uwjp1a`)
2. **Files Touched**: `outlier_scrapers/probability_blend.py`, `outlier_scrapers/feedback.py`, `outlier_scrapers/projections.py`, `outlier_scrapers/totals_model.py`, `outlier_scrapers/team_strength.py` (new), `config/fundamentals_promotion.json` (new), plus matching tests, and this handoff file. PR (merged): `https://github.com/DaSilvaDub/outlier/pull/129`
3. **Next Steps**: All four phases of the totals-pipeline audit landed and merged.
   - **Phase 1** (`probability_blend.py`): `fit_weight_artifact` now does a per-segment chronological train/holdout split — a segment only unlocks a weight below `DEFAULT_MARKET_WEIGHT_FLOOR` (0.75) once it beats the market-only Brier score OOS on the held-out slice, otherwise it's floored (raw in-sample fit kept as `raw_market_weight`). Also quarantines the known 2026-07 column-misalignment rows (`market_type`/`time_before_game` as stringified percentiles) and raised `min_samples` 30→200. **Post-review fix**: `_apply_floor()` originally published the all-data fit (which includes the holdout rows) whenever holdout improved, instead of `holdout["trained_market_weight"]` (the train-only weight actually scored) — a holdout-label leak. Fixed so the published weight is always the train-only validated weight.
   - **Phase 2** (`feedback.py`): added `totals_paired_oos_loss()` (paired Brier-loss d_t = loss(L10) − loss(market) with 95% CI) and `totals_edge_bucket_report()` (signed edge-band hit rates with Wilson CIs + monotonicity check), plus persisting `recency_hit_prob` on `market_snapshots` (it was computed but never saved before). **Post-review fix**: `_is_totals_row()` originally only matched `totals_model.SOURCE_DEVIG`/`SOURCE_BLEND` (the backfill path), missing the specialized totals board's actual `model_prob_source` values (`"book_median"`/`"single_book"`/`"totals_model"` via `game_totals.py`→`pack.py:normalize_original`) — the harness was silently skipping most real totals rows. Fixed by widening `TOTALS_MODEL_PROB_SOURCES`.
   - **Phase 3** (`projections.py`, `totals_model.py`): added `mlb_game_total_runs_distribution()` (NB2), `standardized_edge()`, and dispersion-fitting helpers; wired as an audit-only diagnostic (`totals_fair_total`/`totals_projection_mean`/`totals_projection_sigma`/`totals_standardized_edge`) into `build_totals_prob_index()`/`backfill_totals_probabilities()`. Never touches `p_over`/`model_prob`/sizing. **Post-review fix**: the diagnostic was applying MLB-scale NB2 dispersion to every league's game totals (a WNBA ladder got `projection_sigma≈48`, meaningless) — gated to `league == "MLB"` only.
   - **Phase 4** (`team_strength.py`, new): opponent-adjusted offense/defense ridge model (pure Python, no numpy), `predict_team_run_mean`/`predict_game_total_mean`, and a shadow-only promotion gate (`config/fundamentals_promotion.json`, mirrors `probability_blend`'s shadow/live pattern, `min_eligible_games=2000` ≈ one MLB season). Deliberately infrastructure-only per user decision — not wired into `totals_model.py` or fed real games yet, since settled history (~3 weeks) is far short of the audit's own "≥1 full season" gate for trusting a fundamentals fit.
   - **Not started**: wiring Phase 3/4 diagnostics into `CANDIDATES_HEADER`/ledger persistence (they're currently row-dict-only, dropped by `extrasaction="ignore"` CSV writers); fetching real settled games to actually fit `team_strength.py` against; any promotion decision for either the blend or fundamentals model (both remain shadow, correctly, until OOS evidence accumulates).
   - Verified at merge: full `pytest tests/` — only the 14 pre-existing failures from missing optional deps (`google`, `openai`, `anthropic`, `dateutil`) in this sandbox, unrelated to this work; `ruff check` and `mypy` clean on all touched files; CI green (`test`, `typecheck`) on the merged head.
