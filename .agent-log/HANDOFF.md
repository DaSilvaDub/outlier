- **Last Commit SHA**: 98e750f (branch `claude/fix-this-3b8abd`, [PR #107](https://github.com/DaSilvaDub/outlier/pull/107))
- **Files Touched**: outlier_scrapers/probable_pitchers.py, outlier_scrapers/refresh.py, outlier_scrapers/projections.py, outlier_scrapers/paths.py, outlier_scrapers/daily_job.py, tests/test_probable_pitchers.py, tests/test_projections.py
- **Next Steps**: PR #107 now bundles both halves of the audit finding that `independent_model_prob` coverage on MLB SO rows was near-zero in production. (1) Root cause: `probable_pitchers.py` had no auto-advance-to-tomorrow logic like `games.py`, so a nightly refresh run late at night kept matching SO markets against yesterday's already-finished slate's starters. Fixed with `_slate_is_complete()` + auto-advance, and `refresh.py` no longer drops an explicit `--date` pin for this producer. Verified live: SF's 2026-08-23 starter now resolves correctly and independent_model_prob went 0/6 → 5/6 after a pack rebuild. (2) The file-based path (`<sport>_projections_latest.json` / `packs/<date>/projections.jsonl`, and the `backfill`/`train`/`validate`/`project` CLI) was genuinely dead — nothing ever invoked it. Wired it up: `export_projections()`/`build_mlb_so_projections()` in projections.py compute starter-SO projections from the already-refreshed props + probable-pitchers files, reusing the exact same `mlb_so_projection_record()` pack.py falls back to inline, so the two paths can never disagree. New `export` CLI subcommand, `--projections` refresh.py flag, and a daily_job.py pipeline step right after `--probable-pitchers`. Scoped to MLB only (matches the HOUSE RULE recommendable-model list). Verified against real data copied into the worktree: 343 SO rows → 314 gamelog-eligible + 29 audit-only-stub projections, exactly matching confirmed-starter gamelog coverage. Full test suite: 1130 passed, 1 pre-existing unrelated failure (`test_alt_bankroll_props.py::test_write_pack_emits_player_and_league_bankroll_csvs`, confirmed via git stash it fails without this branch too). PR #107 open against master, not yet merged.
- **Process note for the next agent**: this worktree's shell cwd gets reset to the worktree path after running the mandatory `report-sync.ps1`, but it's easy to keep issuing `cd "C:\...\outlier"` (canonical) in later Bash calls out of habit — that silently routes Edit/Read/Bash calls to canonical `master` instead of the worktree/branch. Happened twice this session; caught both times via `git status --short` in both locations before committing. Always verify `git status`/`git branch --show-current` in the *worktree* right before committing, and double-check any saved `git diff` patch actually has content before relying on it to recover from a wrong-directory edit.
